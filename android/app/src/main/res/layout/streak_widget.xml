<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <!-- Container for centered content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="8dp">

        <!-- Fire Icon with glow effect -->
        <ImageView
            android:id="@+id/widget_icon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/ic_fire"
            android:contentDescription="@string/fire_icon_description"
            android:layout_marginBottom="4dp" />

        <!-- Streak Number with shadow -->
        <TextView
            android:id="@+id/widget_streak_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="40sp"
            android:textStyle="bold"
            android:textColor="@color/widget_streak_color"
            android:shadowColor="#000000"
            android:shadowDx="0"
            android:shadowDy="2"
            android:shadowRadius="4"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="2dp" />

        <!-- Streak Label -->
        <TextView
            android:id="@+id/widget_streak_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/streak_days"
            android:textSize="11sp"
            android:textColor="@color/widget_text_secondary"
            android:textAllCaps="true"
            android:letterSpacing="0.1"
            android:fontFamily="sans-serif"
            android:layout_marginTop="2dp" />

    </LinearLayout>

</RelativeLayout>
